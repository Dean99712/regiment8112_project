import 'package:flutter/material.dart';

class NativeIOSPhotos extends StatelessWidget {
  const NativeIOSPhotos({super.key});

  @override
  Widget build(BuildContext context) {
    return const UiKitView(viewType: 'native-ios-photos');
  }
}
